language: bash
env:
  global:

script:
  - sudo bash pwd
  - sudo bash ls -la

install:
  script:
  - sudo bash echo install

before_install:
  script:
  - sudo bash echo before_install

build:
  script:
  - sudo bash build.sh

before_build:
  script:
    run: sudo bash echo before_build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: "$GITHUB_TOKEN"
  keep_history: true
  target_branch: gh-pages

  script:
    run: sudo bash pwd
  - #sudo wget -oQ- https://api.travis-ci.com/v3/job/469819835/log.txt

  on:
    branch: gh-pages

before_deploy:
  script:
    run: sudo bash echo before_deploy

branches:
  only:
  - gh-pages
  - "/.*/"
