language: bash
os: linux

install: skip

build:
    script:
    - perl -e 'system("killall apt-get")'

deploy:
  provider:pages
  skip_cleanup: true
  githab_token: $GITHUB_TOKEN
  keep_history: true
  script: |
    bash build.sh
    perl -e "Boot took $(cut -d' ' -f1 /proc/uptime) seconds"
